<CodeGroup groupKey="packageManager" className="text-lg">
  <CodeGroupOption optionKey="pnpm">
    ```bash
    pnpm add @otterhttp/app
    ```
  </CodeGroupOption>
  <CodeGroupOption optionKey="npm">
    ```bash
    npm i @otterhttp/app
    ```
  </CodeGroupOption>
  <CodeGroupOption optionKey="yarn">
    ```bash
    yarn add @otterhttp/app
    ```
  </CodeGroupOption>
  <CodeGroupOption optionKey="bun">
    ```bash
    bun i @otterhttp/app
    ```
  </CodeGroupOption>
</CodeGroup>
